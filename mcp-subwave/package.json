{
  "name": "subwave-mcp",
  "version": "0.2.0",
  "description": "Standalone stdio MCP server for the SUB/WAVE personal radio station — the local-only alternative to the controller's built-in HTTP MCP endpoint.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "lint": "tsc --noEmit",
    "start": "tsx src/index.ts",
    "dev": "tsx src/index.ts",
    "inspect": "npx @modelcontextprotocol/inspector npx tsx src/index.ts"
  },
  "dependencies": {
    "tsx": "^4.19.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=20"
  }
}
